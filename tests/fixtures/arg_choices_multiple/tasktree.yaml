tasks:
  deploy:
    args:
      - environment: { choices: ["dev", "staging", "prod"] }
      - region: { choices: ["us-east-1", "eu-west-1"] }
    outputs: [deploy.log]
    cmd: python3 -c "open('deploy.log', 'w').write('{{ arg.environment }} in {{ arg.region }}\n')"
