tasks:
  deploy:
    args: [environment]
    outputs: ["deploy-{{ arg.environment }}.log"]
    cmd: python3 -c "open('deploy-{{ arg.environment }}.log', 'w').write('Deployed to {{ arg.environment }}\n')"
