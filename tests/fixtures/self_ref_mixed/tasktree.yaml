tasks:
  transform:
    inputs:
      - source: data.txt
    outputs:
      - target: processed.txt
    cmd: python3 -c "import pathlib; pathlib.Path('{{ self.outputs.target }}').write_text(pathlib.Path('{{ self.inputs.source }}').read_text().upper())"
