runners:
  default: default_runner
  default_runner:
    interpreter:
      cmd: bash
  cli_runner:
    interpreter:
      cmd: zsh
imports:
  - file: build.yaml
    as: build
    run_in: default_runner
tasks:
  root_task:
    cmd: echo 'root task'
