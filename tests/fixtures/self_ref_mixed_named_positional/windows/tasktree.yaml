tasks:
  process:
    inputs:
      - config: named.txt
      - anon.txt
    outputs:
      - result: output.txt
      - debug.log
    cmd: |
      copy /b {{ self.inputs.config }}+{{ self.inputs.1 }} {{ self.outputs.result }}
      echo Processed > {{ self.outputs.1 }}
