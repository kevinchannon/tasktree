imports:
  - file: base.yaml
    as: base

tasks:
  setup:
    deps: [base.config]
    outputs: [setup.txt]
    cmd: python3 -c "open('setup.txt', 'w').write('step 3\n')"

  prepare:
    deps: [setup]
    outputs: [prepare.txt]
    cmd: python3 -c "open('prepare.txt', 'w').write('step 4\n')"
