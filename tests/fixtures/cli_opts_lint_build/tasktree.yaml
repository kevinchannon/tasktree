tasks:
  lint:
    outputs: [lint.log]
    cmd: python3 -c "open('lint.log', 'w').write('linting\n')"

  build:
    deps: [lint]
    outputs: [build.log]
    cmd: python3 -c "open('build.log', 'w').write('building\n')"
