runners:
  docker_runner:
    type: containerised
    engine: docker
    dockerfile: Dockerfile
    context: .

tasks:
  docker_task:
    run_in: docker_runner
    pin_runner: true
    cmd: echo 'Running in Docker'
