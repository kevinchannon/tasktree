runners:
  default: recipe-default
  recipe-default:
    interpreter:
      cmd: bash
      preamble: echo "RECIPE DEFAULT"

  task-specific:
    interpreter:
      cmd: bash
      preamble: echo "TASK SPECIFIC"

  cli-override:
    interpreter:
      cmd: bash
      preamble: echo "CLI OVERRIDE"

tasks:
  test:
    runner: task-specific
    cmd: echo "Hello"
