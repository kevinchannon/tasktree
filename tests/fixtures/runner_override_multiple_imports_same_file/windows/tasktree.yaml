runners:
  runner_a:
    interpreter:
      cmd: cmd.exe
      ext: .bat
  runner_b:
    interpreter:
      cmd: cmd.exe
      ext: .bat
imports:
  - file: shared.yaml
    as: set_a
    run_in: runner_a
  - file: shared.yaml
    as: set_b
    run_in: runner_b
tasks:
  all:
    deps: [set_a.task1, set_b.task1]
    cmd: echo done
