runners:
  runner_a:
    shell:
      cmd: [cmd.exe, /c]
  runner_b:
    shell:
      cmd: [cmd.exe, /c]
imports:
  - file: shared.yaml
    as: set_a
    run_in: runner_a
  - file: shared.yaml
    as: set_b
    run_in: runner_b
tasks:
  all:
    deps: [set_a.task1, set_b.task1]
    cmd: echo done
