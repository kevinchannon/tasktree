runners:
  default: custom
  custom:
    shell:
      cmd: [bash]
      preamble: echo "RECIPE DEFAULT"

  task-specific:
    shell:
      cmd: [bash]
      preamble: echo "TASK SPECIFIC"

tasks:
  test:
    run_in: task-specific
    cmd: echo "Hello"
