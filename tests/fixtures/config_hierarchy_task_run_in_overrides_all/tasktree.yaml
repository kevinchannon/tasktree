runners:
  default: custom
  custom:
    shell:
      cmd: [bash, -c]
      preamble: echo "RECIPE DEFAULT"

  task-specific:
    shell:
      cmd: [bash, -c]
      preamble: echo "TASK SPECIFIC"

tasks:
  test:
    run_in: task-specific
    cmd: echo "Hello"
