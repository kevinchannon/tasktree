runners:
  default: custom
  custom:
    interpreter:
      cmd: bash
      preamble: echo "RECIPE DEFAULT"

  task-specific:
    interpreter:
      cmd: bash
      preamble: echo "TASK SPECIFIC"

tasks:
  test:
    runner: task-specific
    cmd: echo "Hello"
