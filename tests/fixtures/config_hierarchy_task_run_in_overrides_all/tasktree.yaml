runners:
  default: custom
  custom:
    interpreter:
      cmd: bash
      preamble: echo "RECIPE DEFAULT"

  task-specific:
    interpreter:
      cmd: bash
      preamble: echo "TASK SPECIFIC"

tasks:
  test:
    run_in: task-specific
    cmd: echo "Hello"
