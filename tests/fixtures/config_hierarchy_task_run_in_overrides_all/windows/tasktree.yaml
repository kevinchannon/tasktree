runners:
  default: custom
  custom:
    interpreter:
      cmd: cmd.exe
      ext: .bat
      preamble: echo RECIPE DEFAULT

  task-specific:
    interpreter:
      cmd: cmd.exe
      ext: .bat
      preamble: echo TASK SPECIFIC

tasks:
  test:
    run_in: task-specific
    cmd: echo Hello
