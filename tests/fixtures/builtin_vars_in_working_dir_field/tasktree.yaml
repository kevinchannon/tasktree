tasks:
  build-task:
    working_dir: "{{ tt.task_name }}"
    cmd: |
      echo "task={{ tt.task_name }}" > {{ tt.project_root }}/result.txt
      echo "wd={{ tt.working_dir }}" >> {{ tt.project_root }}/result.txt
