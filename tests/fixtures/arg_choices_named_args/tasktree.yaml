tasks:
  deploy:
    args:
      - app_name: { type: str }
      - environment: { choices: ["dev", "staging", "prod"] }
    outputs: [deploy.log]
    cmd: python3 -c "open('deploy.log', 'w').write('{{ arg.app_name }} to {{ arg.environment }}\n')"
