tasks:
  test-vars:
    cmd: python3 -c "open('{{ tt.project_root }}/output.txt', 'w').write('project_root={{ tt.project_root }}\nrecipe_dir={{ tt.recipe_dir }}\ntask_name={{ tt.task_name }}\nworking_dir={{ tt.working_dir }}\ntimestamp={{ tt.timestamp }}\ntimestamp_unix={{ tt.timestamp_unix }}\nuser_home={{ tt.user_home }}\nuser_name={{ tt.user_name }}\n')"
