variables:
  version: "1.0"

tasks:
  process:
    inputs:
      - src: "src/app-{{ var.version }}.txt"
    outputs:
      - dest: output.txt
    cmd: python3 -c "import shutil; shutil.copy('{{ self.inputs.src }}', '{{ self.outputs.dest }}')"
