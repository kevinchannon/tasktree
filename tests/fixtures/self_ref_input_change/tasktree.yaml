tasks:
  process:
    inputs:
      - source: data.txt
    outputs:
      - result: processed.txt
    cmd: python3 -c "import shutil; shutil.copy('{{ self.inputs.source }}', '{{ self.outputs.result }}')"
