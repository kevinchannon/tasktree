variables:
  base_url: "https://api.example.com"
  endpoint: { env: TEST_OVERRIDE, default: "{{ var.base_url }}/users" }

tasks:
  test:
    outputs: [config.txt]
    cmd: python3 -c "open('config.txt', 'w').write('Endpoint={{ var.endpoint }}\n')"
