variables:
  server: "prod.example.com"

tasks:
  deploy:
    args: [app_name]
    outputs: ["deploy.log"]
    cmd: python3 -c "open('deploy.log', 'w').write('Deploy {{ arg.app_name }} to {{ var.server }} as {{ env.USER }}\n')"
