runners:
  default: default_runner
  default_runner:
    interpreter:
      cmd: bash
  blanket_runner:
    interpreter:
      cmd: zsh
  cli_runner:
    interpreter:
      cmd: sh
imports:
  - file: build.yaml
    as: build
    run_in: blanket_runner
tasks:
  default_task:
    cmd: echo 'default'
