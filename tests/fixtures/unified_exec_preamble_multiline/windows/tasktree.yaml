runners:
  strict:
    interpreter:
      cmd: cmd.exe /c
      ext: .bat
      preamble: |
        set MY_VAR=preamble_value

tasks:
  test-multi-preamble:
    runner: strict
    cmd: |
      echo Start>output.txt
      echo %MY_VAR%>>output.txt
      echo End>>output.txt
