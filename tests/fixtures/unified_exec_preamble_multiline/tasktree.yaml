runners:
  strict:
    interpreter:
      cmd: bash
      preamble: |
        set -e
        MY_VAR="preamble_value"

tasks:
  test-multi-preamble:
    runner: strict
    cmd: |
      echo "Start" > output.txt
      echo "$MY_VAR" >> output.txt
      echo "End" >> output.txt
