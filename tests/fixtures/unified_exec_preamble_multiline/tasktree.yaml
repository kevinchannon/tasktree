runners:
  strict:
    shell:
      cmd: [bash, -c]
      preamble: |
        set -e
        MY_VAR="preamble_value"

tasks:
  test-multi-preamble:
    run_in: strict
    cmd: |
      echo "Start" > output.txt
      echo "$MY_VAR" >> output.txt
      echo "End" >> output.txt
