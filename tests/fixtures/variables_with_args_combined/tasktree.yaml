variables:
  server: "prod.example.com"
  port: 8080

tasks:
  deploy:
    args: [app_name]
    outputs: ["deploy-{{ arg.app_name }}.log"]
    cmd: python3 -c "open('deploy-{{ arg.app_name }}.log', 'w').write('Deploy {{ arg.app_name }} to {{ var.server }}:{{ var.port }}\n')"
