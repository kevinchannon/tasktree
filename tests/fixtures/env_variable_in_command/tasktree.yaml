variables:
  api_key: { env: TEST_API_KEY }
  db_host: { env: TEST_DB_HOST }
  connection: "{{ var.db_host }}:5432"

tasks:
  test:
    outputs: [config.txt]
    cmd: python3 -c "open('config.txt', 'w').write('API={{ var.api_key }} DB={{ var.connection }}\n')"
