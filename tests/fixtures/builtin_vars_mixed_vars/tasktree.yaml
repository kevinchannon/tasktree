variables:
  server: prod.example.com

tasks:
  deploy:
    args: [region]
    cmd: python3 -c "open('{{ tt.project_root }}/mixed.txt', 'w').write('Deploying from {{ tt.project_root }}\nTask: {{ tt.task_name }}\nServer: {{ var.server }}\nRegion: {{ arg.region }}\nUser: {{ tt.user_name }}\n')"
