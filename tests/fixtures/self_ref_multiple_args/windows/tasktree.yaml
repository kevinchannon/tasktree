tasks:
  package:
    args: [version, channel]
    inputs:
      - lib: src/{{ arg.version }}/{{ arg.channel }}/lib.js
    outputs:
      - bundle: dist-{{ arg.version }}-{{ arg.channel }}.js
    cmd: copy /b /y {{ self.inputs.lib }} {{ self.outputs.bundle }}
