imports:
  - file: base.yaml
    as: base

tasks:
  prepare:
    deps: [base.setup]
    outputs: [common-output.txt]
    cmd: python3 -c "open('common-output.txt', 'w').write('common prepare complete\n')"
