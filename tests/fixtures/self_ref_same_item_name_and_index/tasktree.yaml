tasks:
  duplicate:
    inputs:
      - source: data.txt
    outputs: [copy1.txt, copy2.txt]
    cmd: python3 -c "import shutil; shutil.copy('{{ self.inputs.source }}', '{{ self.outputs.0 }}'); shutil.copy('{{ self.inputs.0 }}', '{{ self.outputs.1 }}')"
