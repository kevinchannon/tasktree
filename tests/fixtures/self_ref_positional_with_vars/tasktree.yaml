variables:
  version: "1.0"

tasks:
  process:
    inputs: ["file-{{ var.version }}.txt"]
    outputs: [result.txt]
    cmd: python3 -c "import shutil; shutil.copy('{{ self.inputs.0 }}', '{{ self.outputs.0 }}')"
