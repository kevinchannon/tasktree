runners:
  strict:
    interpreter:
      cmd: cmd.exe
      ext: .bat
      preamble: |
        set TEST_VAR=from_preamble

tasks:
  test-preamble:
    run_in: strict
    cmd: echo %TEST_VAR%>output.txt
