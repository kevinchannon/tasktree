runners:
  strict:
    interpreter:
      cmd: cmd.exe /c
      ext: .bat
      preamble: |
        set TEST_VAR=from_preamble

tasks:
  test-preamble:
    runner: strict
    cmd: echo %TEST_VAR%>output.txt
