runners:
  strict:
    shell:
      cmd: [bash, -c]
      preamble: |
        set -e
        export TEST_VAR="from_preamble"

tasks:
  test-preamble:
    run_in: strict
    cmd: python3 -c "import os; open('output.txt', 'w').write(os.environ.get('TEST_VAR', '') + chr(10))"
