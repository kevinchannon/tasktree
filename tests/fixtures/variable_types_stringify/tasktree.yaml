variables:
  port: 8080
  debug: true
  timeout: 30.5

tasks:
  test:
    outputs: [config.txt]
    cmd: python3 -c "open('config.txt', 'w').write('port={{ var.port }} debug={{ var.debug }} timeout={{ var.timeout }}\n')"
